---
name: "source-command-serve-changes"
description: "Deprecated after Pediment became a plugin-only product."
---

# source-command-serve-changes

Use this skill when the user asks to run the migrated source command `serve-changes`.

## Command Template

# Deprecated

There is no parent-theme checkout to fast-forward. Build the plugin with
`npm run build`; test it with the fixture client theme or the target site's
standalone client theme.
